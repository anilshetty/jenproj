pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python3 --version'
                sh 'echo "Hello world! welcome to continous integration"
            }
        }
    }
}
